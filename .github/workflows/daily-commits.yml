name: Daily Commits

on:
  schedule:
    # 매일 23:00 UTC = 08:00 KST(아침 8시)에 실행
    - cron: '0 23 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure git
        run: |
          git config user.name "323nimiji0hc"
          git config user.email "296764738+323nimiji0hc@users.noreply.github.com"

      - name: Create 10 commits
        run: |
          mkdir -p .daily
          LOG=".daily/activity.txt"
          for i in $(seq 1 10); do
            echo "$(date -u +'%Y-%m-%dT%H:%M:%SZ') commit #$i" >> "$LOG"
            git add "$LOG"
            git commit -m "chore: daily activity $(date -u +'%Y-%m-%d') #$i"
          done

      - name: Push
        run: git push
